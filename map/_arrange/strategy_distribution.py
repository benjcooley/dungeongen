"""Strategy distribution configuration."""
from map._arrange.room_distribution import ROOM_DISTRIBUTION

# Re-export room distribution as strategy distribution for backwards compatibility
STRATEGY_DISTRIBUTION = ROOM_DISTRIBUTION
